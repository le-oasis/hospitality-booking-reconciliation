name: 'hospitality_analytics'
version: '1.0.0'
config-version: 2

profile: 'hospitality'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  hospitality_analytics:
    staging:
      +materialized: view
      +schema: staging
    reporting:
      +materialized: table
      +schema: reporting
